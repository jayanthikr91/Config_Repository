node{

checkout scm;
def prop=readProperties  file: 'Properties/pipeline.properties'

stage('util clone')

{
sh "rm -rf ../${JOB_NAME}/*/"
sh prop.UTIL_REPO_URL
echo "cloned successfully"
utilrepo=load "Util_Repository/dev-cicd.groovy"
utilrepo.execute();  
}
}
